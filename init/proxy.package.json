{
  "name": "prestruct-proxy",
  "version": "0.1.0",
  "description": "Prestruct bot-render proxy. VPS (Node/Express/Puppeteer) or Cloudflare Worker.",
  "type": "module",
  "scripts": {
    "start":  "node scripts/proxy.js",
    "dev":    "node --watch scripts/proxy.js"
  },
  "dependencies": {
    "express":   "^4.19.2",
    "puppeteer": "^22.0.0"
  },
  "devDependencies": {
    "@cloudflare/puppeteer": "^0.0.5",
    "wrangler": "^3.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
